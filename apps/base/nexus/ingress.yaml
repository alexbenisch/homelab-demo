apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nexus-ui
  annotations:
    external-dns.alpha.kubernetes.io/hostname: nexus.k8s-demo.de
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
spec:
  rules:
    - host: nexus.k8s-demo.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: nexus
                port:
                  number: 8081
