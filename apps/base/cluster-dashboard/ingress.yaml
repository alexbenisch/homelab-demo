apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cluster-dashboard
  annotations:
    external-dns.alpha.kubernetes.io/hostname: dashboard.k8s-demo.de
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
spec:
  rules:
    - host: dashboard.k8s-demo.de
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cluster-dashboard
                port:
                  number: 80
