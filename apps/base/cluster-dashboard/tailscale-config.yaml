apiVersion: v1
kind: Service
metadata:
  name: cluster-dashboard-tailscale
  annotations:
    tailscale.com/hostname: cluster-dashboard
spec:
  type: LoadBalancer
  loadBalancerClass: tailscale
  selector:
    app: cluster-dashboard
  ports:
    - name: https
      port: 443
      targetPort: 8000
