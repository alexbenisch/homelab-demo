apiVersion: v1
kind: Service
metadata:
  name: cluster-dashboard
spec:
  type: ClusterIP
  selector:
    app: cluster-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
